# Backend Environment Configuration Example
# Copy this file to `.env` and adjust values for your environment

# ============================================================================
# Environment & Logging
# ============================================================================

# Environment mode
# development  = DEBUG logging (verbose, structured output with emojis)
# production   = INFO logging (minimal, production-optimized)
ENV=development

# Log level threshold
# DEBUG   = Show all logs (development)
# INFO    = Show info, warning, error (production)
# WARNING = Show warning and error only
# ERROR   = Show errors only
LOG_LEVEL=DEBUG

# ============================================================================
# CORS Configuration
# ============================================================================

# Comma-separated list of allowed origins (Frontend URLs)
# These URLs are whitelisted for Cross-Origin requests
# 
# Local Development:
#   - http://localhost:3000       (Next.js dev server)
#   - tauri://localhost           (Tauri Desktop App)
#   - http://192.168.2.116:3000   (LAN access)
#
# Production Examples:
#   - https://app.billino.de      (Single HTTPS domain)
#   - https://app1.example.com,https://app2.example.com  (Multiple domains)
ALLOWED_ORIGINS=http://localhost:3000,tauri://localhost,http://192.168.2.116:3000

# ============================================================================
# Production Configuration (Commented - Uncomment for Deployment)
# ============================================================================

# ENV=production
# LOG_LEVEL=INFO
# ALLOWED_ORIGINS=https://app.billino.de
