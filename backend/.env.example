# Backend Environment Configuration Example
# Copy this file to `.env` and adjust values for your environment

# ============================================================================
# Environment & Logging
# ============================================================================

# Environment mode
# development  = DEBUG logging (verbose, structured output with emojis)
# production   = INFO logging (minimal, production-optimized)
ENV=development

# Log level threshold
# DEBUG   = Show all logs (development)
# INFO    = Show info, warning, error (production)
# WARNING = Show warning and error only
# ERROR   = Show errors only
LOG_LEVEL=DEBUG

# ============================================================================
# CORS Configuration
# ============================================================================

# Comma-separated list of allowed origins (Frontend URLs)
# These URLs are whitelisted for Cross-Origin requests
# 
# Local Development:
#   - http://localhost:3000       (Next.js dev server)
#   - tauri://localhost           (Tauri Desktop App)
#   - http://192.168.2.116:3000   (LAN access)
#
# Production Examples:
#   - https://app.billino.de      (Single HTTPS domain)
#   - https://app1.example.com,https://app2.example.com  (Multiple domains)
ALLOWED_ORIGINS=http://localhost:3000,tauri://localhost,http://192.168.2.116:3000

# ============================================================================
# Backup Configuration
# ============================================================================

# Backup-System aktivieren
# true  = Backups aktiv (empfohlen)
# false = Backups deaktiviert
BACKUP_ENABLED=true

# Tägliches Backup-Zeitpunkt (UTC)
# Hour: 0-23, Minute: 0-59
# Standard: 02:00 UTC (nachts in Europa)
BACKUP_SCHEDULE_HOUR=2
BACKUP_SCHEDULE_MINUTE=0

# Tage, bis alte Backups automatisch gelöscht werden
# Standard: 30 Tage (verwaltet Speicherplatz)
BACKUP_RETENTION_DAYS=30

# Backup-Verzeichnis (relativ zu Backend-Root)
# Standard: ./data/backups (im data-Ordner, neben DB)
BACKUP_PATH=./data/backups

# Tauri Desktop-App Modus
# true  = Tauri-App läuft (spezielle Backup-Handling später)
# false = Basic-Modus (Standard, HTTP-basiert)
TAURI_ENABLED=false

# ============================================================================
# Production Configuration (Commented - Uncomment for Deployment)
# ============================================================================

# ENV=production
# LOG_LEVEL=INFO
# ALLOWED_ORIGINS=https://app.billino.de
# BACKUP_ENABLED=true
# BACKUP_SCHEDULE_HOUR=2
# BACKUP_SCHEDULE_MINUTE=0
# BACKUP_RETENTION_DAYS=30
# BACKUP_PATH=./backups
# TAURI_ENABLED=false
