name: Tauri Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  tauri-build:
    name: Build Tauri Desktop App (Skipped)
    runs-on: ubuntu-latest

    steps:
      - name: Skip Tauri build
        run: |
          echo "⏭️ Tauri build is currently skipped"
          echo "This workflow will be enabled when Tauri implementation is ready"
